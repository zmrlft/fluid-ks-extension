import React from 'react';
import styled from 'styled-components';

const Wrapper = styled.h3`
  margin: 8rem auto;
  text-align: center;
`;

export default function App() {
  return <Wrapper>Fluid, elastic data abstraction and acceleration for BigData&#x2F;AI applications in cloud.</Wrapper>;
}
